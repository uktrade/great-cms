# Great Avatar

```<great-avatar>``` is a custom web component that extends HTMLElement it accepts won't pass any attribute down except ```src```
- src - absolute or relative path to an image/svg or alternatively a base64 image string 

NOTE: Passing custom CSS class or inline style won't take effect as the button comes pre styled

# Great Link

`<great-link>` is a custom web component that extends HTMLElement it accepts all attributes that you would pass to a `<a>` plus the following custom ones:

-   href - link path to the page

NOTE: Passing custom CSS class or inline style won't take effect as the button comes pre styled, to change the button appearance please pass `theme` attribute

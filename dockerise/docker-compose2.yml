version: '3.4'

services:
  pgsql:
    container_name: great_postgres
    image: postgres:12
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    volumes:
      - ./docker_data/initdb.sh/:/docker-entrypoint-initdb.d/
      - dbfiles:/var/lib/postgresql/data
    ports:
      - '5432:5432'
    networks:
      - inside-network

volumes:
  dbfiles:

networks:
  inside-network:
    driver: bridge
